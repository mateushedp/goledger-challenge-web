export type TvShow = {
  key: string;
  title: string;
  description: string;
  recommendedAge: number;
};

export type TvShowInput = {
  title: string;
  description: string;
  recommendedAge: number;
};